################################################################################
# SESSION 2026-09-09  --  TERM 3 RE-BASELINE (branch: term3, off attach-week9)
# (This block closes the documentation gap for the 2026-08-05..14 work, whose
#  full write-up lives in update.txt sections 10-12 -- read that for the detach
#  hand-back campaign and the attach fault teardown. Strategy re-baseline:
#  general/TERM3_STRATEGY.md.)
################################################################################

WHAT THE AUG 6-9 CAMPAIGNS ESTABLISHED (detail in update.txt)
  * DETACH VIA OCP FLEET-RESIZE BEATS THE NETWORK HAND-OFF. reconfig_mode:=ocp
    (runtime rho/cable-len as acados params, prebuilt per-size solvers, survivors
    keep their true uneven azimuths) vs :=network, one-flag A/B R0111-R0114:
    settled load tilt ~1 deg vs 26-48 deg, survivor tracking error ~1 mm vs up to
    0.9 m. Not yet written up as a result; known bug: LAND path aborts on the
    parked detached drone AFTER the trajectory completes.
  * THREE STACKED ATTACH FAULTS FOUND AND FIXED (each masked the next): the mux
    handed authority to an armed-idle stream (motors cut at every weld); the
    approach drone spawned on a corridor through drone 0 (bulldozed it pre-weld);
    the pose watchdog ran on the WALL clock in a 0.3x sim (disarmed healthy
    drones). Fixes: handover_policy.py (live-takeover gate), spawn moved to the
    gap bisector, pose_timeout_s param.
  * THE WELD-COMPLIANCE HYPOTHESIS IS REFUTED. One-factor SDF ladder (ball
    baseline / rod inertia / 2-segment / rigid, generated by
    tools/make_weld_variants.py): the post-weld failure is invariant to joint
    mechanics; rigid is decisively worst (capsize <2 s, 5/5).
  * THE REAL REMAINING ATTACH PROBLEM IS THE ONE-SIDED HAND-OUT: the newcomer
    eases over 12 s but the incumbents' balanced-tension targets step in one tick
    (d0 elev 37->50 deg instantly -> tilt 0.15->38 deg), and the ramp runs away
    at ~75%. Fix = ease the incumbents too (reference-side only).

DECISIONS (Wesley, 2026-09-09)
  * N3 restated: the attachment is modelled to act like a BALL JOINT after the
    weld, exactly like the tethered drones (match the real rig); the rigid-weld
    control problem is out of scope. The rod variant (tether-matched inertia,
    ball joints) is the canonical attach model going forward.
  * Rig access ~2 days/week this term, Tejen likely available -> hardware attach
    stays in scope; H0 bring-up targeted within ~2 weeks.
  * Lit review already written (LaTeX, outside the repo for now).
  * August work committed and pushed on term3 (checkpoint commits, this session).

ALSO THIS SESSION
  * make_weld_variants.py --check wired into gate stage 4 (generated worlds can
    no longer go silently stale).
  * Run archival deferred: real run dirs are MBs-150MB (not the plan's ~0.5 MB
    estimate), so raw-run promotion to results_archive waits for the P2 re-fly
    that produces the actual thesis figures.

################################################################################
# SESSION 2026-08-03  --  REAL-WORLD TAKEOFF BUGS FIXED (branch: attach-week9)
# (Supersedes nothing below; these are NEW findings against the real rig and the
#  dissipative stack. The open dissipative tracking issue is written up in
#  DISSIPATIVE_TRACKING_ISSUE.md -- start there for that thread.)
################################################################################

THE THREE REAL-WORLD SYMPTOMS THAT STARTED THIS -- ALL SOLVED
  1. "MPC could not find a solution for certain starting configurations; moving
     the drones around a bit fixed it. Never happened after takeoff."
     ROOT CAUSE: geometry.azimuth_slot_assignment matched each drone to a ring
     slot using WORLD azimuths, ignoring payload yaw. The slots ARE the attach
     points rho_i, which rotate with the load. Past half a slot pitch (60 deg for
     3 drones) every drone got paired with its NEIGHBOUR's attach point;
     build_x_init then read that cable's direction from the wrong attach point, so
     the implied cable length came out 0.59-0.64 m against a modelled 0.50 m -- a
     20-28% stretch the rigid-cable model cannot represent, and the QP went
     infeasible (acados status 4, ACADOS_MINSTEP). Needs BOTH a yawed payload and
     a low cable elevation (drones on the ground), which is why it only ever bit
     before takeoff and never in the air.
     FIX: match in the LOAD frame (azimuth_slot_assignment now takes load_yaw).
     EVIDENCE: 6 randomly-generated placements, 4 failed the first cold solve
     before / 0 after, implied cable length exactly 0.50 m in all.

  2. "For certain starting configurations the drones flew to predefined locations
     -- the trajectory didn't factor in starting position/yaw."
     ROOT CAUSE: two places hard-coded a world-aligned rig.
       * reference_builder.q_ref_at returned IDENTITY, i.e. it commanded the
         payload's yaw to 0 regardless of how the rig was placed.
       * reference_builder.yref_at used _s_nom -- cable directions built from the
         BODY-frame attach ring -- directly as a WORLD-frame reference. Its weight
         (w_s=25) is the strongest formation term in the OCP cost.
     FIX: latch the payload's measured yaw at the first solve (psi0) and express
     both about it.
     EVIDENCE: demanded formation rotation at takeoff went from up to 65 deg to
     0.0 deg at every payload yaw (0/40/95/140/175).

  3. "One takeoff had a drone do almost a 360 before looking good again."
     ROOT CAUSE: acados._tilt_quat_from_accel returns a YAW-FREE tilt quaternion,
     i.e. a commanded heading of world +x, and the yaw term carries the LARGEST
     attitude weight in the tracker cost (5.0 vs 0.5 for roll/pitch). Any drone
     not physically placed facing +x yaws all the way to 0 during takeoff; near
     180 deg the sign-invariant attitude error is degenerate and it can go the
     long way round.
     FIX: latch each drone's resting heading while it sits armed and compose it
     into q_ref (rotation about BODY z after the tilt, so the thrust axis is
     unchanged).
     EVIDENCE: commanded yaw rate 0.000 at every placement heading; net rotation
     <= 3 deg vs up to 180 deg before.

  WHY SIM NEVER CAUGHT ANY OF THESE: in Gazebo the payload and all drones spawn at
  yaw 0, so psi0 = 0 and every one of the three bugs is the identity.

ALSO FIXED THIS SESSION
  * SIM CLOCK DOMAIN SPLIT (this invalidated earlier sim comparisons).
    controller_mpc.Controller and main.CentralController both pinned
    use_sim_time=False unconditionally -- correct on hardware (no /clock), wrong
    in sim: the trackers ran at 50 Hz WALL while the planner ran at 10 Hz SIM, so
    control updates per SIMULATED second equalled Gazebo's real-time factor, i.e.
    a function of machine load. Two runs of the SAME trajectory at RTF 0.40 and
    0.60 gave payload radius errors of -16% and -34%. Now both honour the launch's
    use_sim_time (sim launches set true, real_* set false, ROS default is false).
    LESSON: before comparing two sim runs, check they took the same wall-clock
    time. If they didn't, the comparison is meaningless.

  * params.json PER RUN. Every run now writes its full ROS parameter set next to
    its logs (utility_objects.data_logger: run_log_dir / write_params /
    node_params). Runs are self-identifying; a whole afternoon was previously lost
    to not knowing which run had which configuration.
      trackers -> c_generated_code_quad_load/logs/controller_quad_load/planner_drone<N>_<ts>/
      planner  -> logs/controller_quad_load/dissipative_controller_<ts>/

  * LAUNCH ARGS THAT WERE NEVER PLUMBED. net_horizon_preview / net_traj_lean were
    node parameters but were not declared or passed in any launch file, so
    "net_traj_lean:=true" was silently dropped and two A/B tests unknowingly
    compared identical configurations. Now declared + passed in all four launches
    that run the dissipative node. ALWAYS verify with `ros2 launch ... --show-args`.

  * RVIZ PAYLOAD PATH. /payload/mpc_plan was (a) a straight tangent line -- 0.56 m
    off a circular path by the end of the 2 s horizon, (b) anchored at the desired
    point so it floated off the payload, and (c) in the network phase computed z
    from the OCP lift schedule, which stops advancing at handover, so during a
    network LAND it reported the load at hover height all the way to touchdown.
    (c) also affected /payload/desired_position, which is what plot_run.py reads.

  * RANDOM WORLD GENERATOR: simulation_assets/generate_random_world.py randomises
    payload yaw + xy, per-drone heading and per-drone azimuth while keeping every
    rod exactly cable_len. generate_rigid_world.py refactored to expose
    build_world(n, placement) so both share the SDF scaffolding. This is what made
    the three bugs above reproducible in sim.

METHOD LESSONS (earned the hard way this session)
  * CHECK THE MAGNITUDE BEFORE BUILDING THE FIX. The "lean" (tilting the cone onto
    g_eff) was built to close an 18.5% radius deficit; the centripetal demand it
    can supply is 0.037 m against a 0.155 m deficit -- it could never have worked,
    and one minute of arithmetic would have shown that.
  * AN OFFLINE HARNESS THAT CANNOT REPRODUCE THE SYMPTOM CANNOT VALIDATE THE CURE.
    mini_plant shows a 1.6% radius deficit where Gazebo shows 18.5%; two fixes
    were shipped on its evidence and both had to be walked back.
  * BEWARE SOLVER STATE BETWEEN OFFLINE TRIALS. An early "24.5% of configurations
    fail" result was pure contamination from acados' internal warm start leaking
    between test cases; under a controlled test the effect was 0%.

################################################################################
# BRANCH: paper-implementation  --  WHAT WAS ACHIEVED
# (This section SUPERSEDES the historical conclusions further down, especially
#  any claim that the coupled OCP is "abandoned/unstable" or that the controller
#  "cannot do agile maneuvers". On this branch the coupled OCP is the working
#  default and tracks moving load trajectories. The old notes are kept below for
#  the debugging story and the still-valid reference material -- KEY NUMBERS,
#  REAL-WORLD TESTING, etc.)
################################################################################

GOAL OF THIS BRANCH
  Make the controller match the paper's actual method as closely as possible:
  an online whole-body kinodynamic OCP planner (planner_mode='coupled',
  planner_ocp.py + load_cable_dynamics.py) that plans the full load+cable+drone
  state over a 2 s horizon and hands each drone a reference trajectory. Exploit
  that in sim we have exact mocap of the payload AND every drone (the paper needs
  an EKF only because it has no load sensors). INDI stays out of scope.

  The coupled OCP already existed but was DISABLED because it diverged; the
  pre-paper working path was 'kinematic' (open-loop, the quasi-static baseline the
  paper argues against). This branch makes the coupled OCP fly stably and become
  the default.

LOCKED SCOPE (agreed up front)
  - Taut air-start only. The paper assumes taut cables throughout and never lifts
    off the ground; the coupled instability lived entirely in the slack->taut
    creep. Use *_rigid_short.sdf with start_taut, handover_elev_deg=0; the creep/
    handover path is bypassed.
  - Hover/setpoint first, agile trajectories second.
  - Keep the existing per-drone MPC tracker. No INDI.

THE 5 ROOT-CAUSE FIXES THAT MADE THE COUPLED OCP FLY
  (each reproduced/validated offline by driving the planner solver directly in
   planner-as-plant closed loops, not guessed)

  1. Mocap state estimator, the paper's way (_build_x_init, planner_node.py).
     Cable DIRECTION s_i is measured from geometry each tick (clean). Cable RATES
     r_i and tensions t_i are RESAMPLED from the previous solution, NOT
     differentiated from mocap -- the paper (Fig 8) avoids differentiating
     estimator values because it is too noisy. (An earlier version measured r_i
     from drone-velocity differences and fed jitter the OCP fought; reverted.)

  2. Partial node-0 pin (planner_ocp.py idxbx_0; observed_state_indices in
     load_cable_dynamics.py; planner_node.py _plan). Pin ONLY the observed states
     at node 0 (load pose/twist + s_i); leave the unobserved tensions and cable
     rates FREE decision variables (warm-started by resampling). Hard-pinning the
     stale resampled tension against a measured pose the tracker did not exactly
     hit made the planned tension RATCHET up to ~8 N until the QP went infeasible
     (acados status 4) and the fleet dropped.

  3. Coordinated-climb reference (_yref_at). Feed the lift velocity forward and
     ramp the height ALONG the horizon. Previously the coupled yref held
     velocity=0 against a rising setpoint, so the load lagged and (via the pin)
     ratcheted tension. Now the load climbs 0.13->0.60 m in ~3 s, tension bounded.

  4. Airborne thrust-gain schedule (controller_quad_load _scheduled_kT;
     thrust_quad_c launch arg). The sim plant is QUADRATIC a(u)=203*u^2, so the
     effective linear gain kT=203*u varies (~20 at takeoff, ~49 at hover). No
     single kT works: 45 over-thrusts at hover (the coupled outer loop turned that
     into a runaway), 50 starves takeoff (drones slid off the stands). Fix: keep
     kT=45 until the drone climbs 0.15 m off the stands, then schedule
     kT=clip(203*throttle, thrust_ratio, 55). Clamped so it can never starve
     takeoff. thrust_quad_c=0 disables.

  5. Formation-holding. The OCP cost had NO term on cable direction and the
     formation radius is a free mode; the drones drifted OUTWARD (elevation
     54->20 deg, tension 1.5->3.9 N, tilting further out -- the "drones tilt a
     lot" symptom). First fixed by raising tension regularisation w_t 0.05->5.0
     (nominal tension IS the 45 deg equilibrium); later subsumed by the explicit
     cable-direction cost (whose hover reference is the nominal 45 deg direction).

RESULT: STABLE COUPLED FLIGHT
  Verified in sim on three_rigid_short.sdf (and 2- and 4-drone worlds): taut
  hover + lift holds, load level (tilt < 1 deg), tensions symmetric (~1.8-2.1 N),
  cable elevation steady ~45 deg, clean DISARM. n=2 and n=4 both work. This is the
  paper's whole-body OCP method flying stably in the sim for the first time.
  Minor, not chased: load settles ~0.53 vs target 0.60 (no integral action; within
  the paper's own tracking RMSE range).

SOLVER-COMPILE CACHE (planner_node.py, planner_ocp.py)
  The planner used to recompile the acados solver EVERY launch (~16 s n=3, ~87 s
  n=4). Added a freshness check + a GEOMETRY SIGNATURE: the .so bakes in n /
  cable_len / load_mass / rho / inertia as compile-time constants, so the cache is
  invalidated when a source file changes OR the geometry changes (a stale .so can
  never fly the wrong geometry). 16.2 s -> 1.2 s on repeat launches.
  Recompiles on: planner_ocp.py/load_cable_dynamics.py edits, or num_drones/
  cable_len/load_mass/inertia changes. Does NOT recompile on thrust_ratio or any
  runtime value (yref, weights).

AGILE TRAJECTORY TRACKING (line_x) + FLATNESS REFERENCE
  - _yref_at(k) now follows a moving load-pose trajectory at each node's horizon
    time (traj_t + k*dt) so the whole 2 s window tracks the moving load; traj_t
    advances in coupled mode once the lift tops out. Reuses the existing sinusoidal
    line_x/circle shapes. At traj_speed 0.2 m/s the load tracks line_x cleanly
    (tension ~1.85 N, tilt < 0.5 deg, elevation 45 deg).
  - Flatness-based cable reference (planner_ocp.py added s_i to the cost;
    _yref_at + rot_align in planner_node.py): a load accelerating at a_load must
    have its cables counter an effective gravity g_eff = g - a_load, so per node
    the cable-direction reference is the nominal formation tilted onto g_eff and
    the tension reference is t_nom*|g_eff|/g. At hover this reduces to nominal
    (so it also pins the formation). This is the paper's approach: derive the
    reference cable state from the load trajectory's acceleration.

  HIGH-SPEED LIMIT (honest finding). At traj_speed >= 0.5 the load lags/
  undershoots. Diagnosed offline: NOT control saturation (|gamma|~0.4 of 50), and
  NOT fixed by the flatness reference in a perfect-tracker model -- reaching full
  amplitude at 0.8 m/s (2.56 m/s^2 lateral) requires pitching the load ~13-15 deg,
  a real position-tracking-vs-load-tilt trade-off tunable via the position weight.
  The flatness reference's real payoff (cables tilt in ANTICIPATION so the drones
  lead the load, cutting REAL tracker lag) cannot appear in a perfect-tracker
  offline model -- it needs sim evaluation. Clean working range today ~0.2-0.4 m/s.
  Pushing toward the paper's 5 m/s needs the position/tilt trade-off tuned and,
  fundamentally, the INDI tracker (deferred) to kill per-drone tracking lag.

KEY FILES CHANGED ON THIS BRANCH
  controller_load_mpc/planner_node.py  -- mocap estimator, _yref_at (climb +
      flatness + line_x), _load_offset_at/_load_accel_at, rot_align, solver cache,
      node-0 partial pin.
  controller_load_mpc/planner_ocp.py   -- partial idxbx_0, cost s_i term, w_t/w_s,
      generate/build flags.
  controller_load_mpc/load_cable_dynamics.py -- observed_state_indices().
  controller_quad_load/controller_mpc.py     -- _scheduled_kT.
  controller_quad_load/launch/mpc_quad_load_launch.py -- planner_mode default
      coupled; thrust_quad_c; taut-start docs.

HOW TO RUN
  ./cleanup.sh
  gz sim simulation_assets/three_rigid_short.sdf -v4 -r
  ros2 launch controller_quad_load rviz_quad_load_launch.py num_drones:=3
  ros2 launch controller_quad_load mpc_quad_load_launch.py num_drones:=3
      # coupled is default; add load_traj:=line_x traj_speed:=0.2 for agility
  Planner-log tells: tilt (load off-vertical, keep small); elev (per-cable, hold
  ~45 deg; collapsing = splay); t= (planned tension, ratcheting up = instability).

OPEN / NEXT STEPS
  - Evaluate the flatness reference in sim at 0.5 m/s (did real lag improve?);
    tune w_s (cable-direction weight, currently 25) if the load pitches too much.
  - figure-eight: same seam, add a shape to _load_offset_at/_load_accel_at.
  - Optional small integral/FF to remove the ~0.07 m load-height offset.
  - Non-equidistant horizon grid (paper p.23), minor.
  - For true high-speed agility: the INDI tracker (deferred).

################################################################################
# HISTORICAL NOTES (pre-paper-implementation branch)
# Kept for the debugging story and reference material. NOTE: conclusions here
# about the coupled OCP being "abandoned" and agile maneuvers being impossible
# are SUPERSEDED by the branch section above.
################################################################################

================================================================================
LEARNING NOTES — Cable-suspended multi-drone load transport (ROS2 + Gazebo)
Goal: replicate the HIGH-LEVEL controller of Sun et al. 2025 (arXiv 2501.18802),
"Agile and Cooperative Aerial Manipulation of a Cable-Suspended Load".
3 quadrotors carry a 0.4 kg payload on soft cables.
Key deviation from paper: we use MPC at the low level, NOT INDI.
================================================================================

--------------------------------------------------------------------------------
SYSTEM OVERVIEW (data flow)
--------------------------------------------------------------------------------
- Gazebo (three_soft_paper.sdf) --pose--> mocap emulator --> /drone_N/motion_capture_state
                                                          --> /payload/motion_capture_state
- Planner (controller_load_mpc) reads all poses, emits per-drone reference:
    /drone_N/reference_trajectory = [pos, vel, a_thrust, a_cable] x (N+1 nodes)
- Tracker (controller_quad_load) x3: per-drone MPC, turns reference into
    /drone_N/ELRSCommand [roll_rate, pitch_rate, throttle, yaw_rate]
- Bridge (simulation_communication) converts ELRS -> Gazebo motor speeds.
- Fleet manager (main.py) owns ARM/TAKEOFF and the /fleet/step clock.
State vector (13): [x y z, qw qx qy qz, vx vy vz, wx wy wz].

================================================================================
PACKAGES & KEY FILES
================================================================================

--- controller_load_mpc  [THE PLANNER — "high-level controller" of the paper] ---
- load_cable_dynamics.py : full coupled load+cable+drone dynamics (CasADi).
    state = load[p,v,q,w] + per-drone[s_i, r_i, ṙ_i, r̈_i, t_i, ṫ_i]; nx=13+14n.
    Helpers: quad_position/velocity (kinematic constraint), thrust_vec (req'd
    thrust force), cable_accel (t_i·s_i/m = cable pull as acceleration).
- planner_ocp.py : acados OCP over that model. Cost tracks LOAD pose/twist +
    tension regulariser + control effort. SQP-RTI, N=20, tf=2s.
- planner_node.py : the running node. Two modes (planner_mode param):
    * 'coupled'   = solve the OCP online every cycle (paper-like). ABANDONED —
                    unstable online (see issues).
    * 'kinematic' = OPEN-LOOP: latch the taut spawn config, lift it rigidly,
                    stream analytic loaded-hover FF. No online OCP, no feedback.
    Also owns: two-phase takeoff (creep->planner), lift ramp, tautness gate.
- diag_equilibrium.py : OFFLINE sanity check (no sim). Builds OCP at a hover
    setpoint, prints tension/FF/geometry consistency. USE THIS to validate math.
  Design decisions:
    * Geometry is ROS params (cable_len, attach_radius, attach_z) — must match
      the .sdf world (cable_len=1.0 for three_soft_paper, 0.6 for three_soft).
    * Cable/thrust FF gated on LOAD being airborne (not cable length) — a taut
      cable to a grounded load bears ~0 tension.
  vs PAPER: paper runs the planner as a trajectory generator + resamples its own
    OCP solution; it does NOT re-solve pinned to noisy measurement each cycle.
    Our 'kinematic' mode is a simplified analytic stand-in (works only for
    hover/lift, not agile trajectories).

--- controller_quad_load  [THE TRACKER — per-drone cable-AWARE MPC] ------------
- dynamics.py : single quad model, nx=17 = [p,q,v,w,u_state]; u_state =
    [roll_rate,pitch_rate,throttle,yaw_rate]. ADDS a_cable term to v̇.
- acados.py : the tracker OCP. NONLINEAR_LS cost on pos/vel/omega/u_state +
    quaternion attitude error. set_planner_reference() converts a_thrust into a
    throttle + tilt-quaternion attitude feedforward (_tilt_quat_from_accel).
- controller_mpc.py : the node. 50 Hz timer -> solve -> publish ELRS.
    diag line: z ez exy rdrift thr roll pitch |aT| |aC|.
    Diagnostic knobs (params): cable_ff_scale (0=cable-blind), attitude_ff.
- main.py : fleet manager (ARM/TAKEOFF, /fleet/step broadcast).
  Design decisions:
    * Separate package from controller_mpc_multi so the cable-free case is
      untouched; own acados codegen dir (c_generated_code_quad_load).
    * Cable enters the MODEL (prediction), not just the cost — the intended
      "piece 1" of the paper (feedforward the known thrust+cable).
  vs PAPER: paper's low level is INDI at >=100Hz (inner loop 300Hz) using a
    MEASURED external force f_ext from the IMU. We use MPC at 50Hz with an
    OPEN-LOOP modelled cable force from the planner. This is the core difference
    and (see issues) likely the root of the instability.

--- controller_mpc_multi  [ORIGINAL cable-BLIND tracker, kept as reference] -----
- Same structure, no cable term. Flies formation circles fine. Used as the
  "known-good" baseline to prove the base MPC/rate-mapping is correct.

--- simulation_communication  [Gazebo <-> ROS bridge] --------------------------
- payload_betaflight_comm.py : /drone_N/ELRSCommand -> Gazebo motor speeds.
- payload_mocap_emulator.py  : Gazebo pose -> /drone_N/motion_capture_state and
    /payload/motion_capture_state (sim stands in for a real MoCap system).
  vs PAPER: paper estimates load pose + cable directions with an EKF; we use
    sim ground-truth mocap (EKF deferred).

--- utility_objects ------------------------------------------------------------
- callback_manager_multi.py : per-drone pub/sub/arming service wiring.
- data_logger.py : CSV logs. visualization.py : RViz.

--- interfaces : custom msgs (MotionCaptureState, ELRSCommand, SetArming). ------
--- controller_mpc / drone_communication / drone_visualisation : legacy / real- -
    hardware / single-drone; not used in the current multi-lift work. -----------

--- worlds (simulation_assets) -------------------------------------------------
- three_soft_paper.sdf : 3 drones spawn ELEVATED + TAUT (1.0 m cables, ~45deg).
    Current test world — removes the slack->taut snap, isolates steady hover.
- three_soft.sdf : 3 drones on the ground, SLACK 0.6 m cables (needs creep).
- *_rigid / *_no_cables : variants for isolation.

================================================================================
KEY NUMBERS
================================================================================
- Payload 0.4 kg; drone 0.6 kg; cable 1.0 m @ 45deg (paper world).
- Thrust model: accel = thrust_ratio*throttle, thrust_ratio=38 (body-z).
- Per-drone thrust authority ~= 0.6*38 = 22.8 m/s^2; minus g leaves ~13 to fight
  the cable. Analytic taut-hover: tension 1.85 N/cable -> a_cable=3.1 m/s^2,
  loaded FF throttle 0.32 + ~10deg outward tilt (|aT|=12.19). All verified
  offline in diag_equilibrium.py.

================================================================================
*** RESOLVED (rigid cables) — READ THIS FIRST ***
================================================================================
- ROOT CAUSE FOUND & FIXED: the tracker instability was the SOFT cable. Soft
  segmented cables act as a position-dependent spring; feeding a CONSTANT
  open-loop cable force (a_cable) into the short-horizon MPC mismatched the real
  (position-varying) force and destabilised the attitude loop -> "sliding away".
- FIX: switched to RIGID massless cables (the reference paper's model) via
  three_rigid.sdf (new; generate_rigid_world.py). Rigid cable tension is ~position
  independent, so the constant a_cable model is valid.
- RESULT: first STABLE lift. Drones hold attitude (roll/pitch ~+/-0.1, never rail),
  the cable term engages smoothly (gate 0->1), the load lifts, and the formation
  recovers radially as the load loads onto the cables.
- Also added takeoff platforms so the (stiff) rigid-cabled drones rest at their
  spawn pose until TAKEOFF instead of collapsing.
- Remaining polish (minor, not blocking): small inward drift (~0.2 m) during the
  pre-load climb that recovers once the cable engages; lift ramp slightly slower
  than the drones climb (ez ~ -0.25). Tunable.
- This is now the paper-faithful config. Next: measured f_ext (IMU) for realism.

================================================================================
PART 2 — MEASURED f_ext FROM IMU (attempt at the paper's reactive cable force)
================================================================================
WHAT WAS BUILT:
- Bridged each drone's Gazebo IMU into ROS. Added an explicit
  <topic>x3_droneN/imu</topic> to models/x3_droneN.sdf so the sensor publishes a
  clean topic even under the nested lift_system world; ros_gz_bridge maps it to
  /drone_N/imu (sensor_msgs/Imu). Verified live.
- measured_cable_accel() in the tracker: the accelerometer reads the body-frame
  SPECIFIC FORCE = (thrust + cable)/m. Subtract the modelled thrust and rotate to
  world:  a_cable_meas = R * (imu_specific_force - [0,0, thrust_ratio*throttle]).
- EMA low-pass (alpha=0.03) on the raw 1 kHz IMU — raw is far too noisy to use
  (|aCm| swung 0.9..4.4 between samples; filtered settles ~1.4..1.7).
- cable_source param (A/B): 'model' = planner open-loop t*s/m (default, unchanged),
  'measured' = IMU f_ext held constant over the MPC horizon (the paper's idea).

WHAT IT REVEALED (good thesis result):
- MEASURED cable accel (~1.5) is ~HALF the open-loop MODEL (3.08) for the whole
  lift. The drones climb AHEAD of the load, so the cables never reach the model's
  assumed design tension -> the open-loop t*s/m is ~2x too high in transient.
  This is exactly why the paper uses a MEASURED reactive f_ext, not a model.
- Corroborated by a persistent ez ~ -0.30 bias: drones ride ~0.3 m above their
  reference, hovering at thr~0.25 while the model predicts 0.32 -> the MPC feedback
  is quietly cancelling the over-large open-loop cable/thrust FF.
- thrust_ratio=38 is confirmed correct (loaded hover wants thr~0.32 = 12.19/38,
  matches the first pre-load sample before the drones outrun the load).

CURRENT BLOCKER (OPEN):
- cable_source:=measured CRASHES within ~1 s. Closing the loop
  IMU -> model -> command -> IMU is unstable on the takeoff transient (most likely
  acados returns a nonzero status and the node raises). Need the paper's safeguards
  before it is usable: (a) clamp the measured magnitude, (b) slew/rate-limit it,
  (c) make a failed solve NON-FATAL (hold last command) so instability shows in the
  diag instead of killing the node, and probably (d) blend model->measured rather
  than switch hard. This is the reactive-inner-loop gap and the next task.

--- ORIGINAL ISSUE WRITE-UP (kept for the debugging story) ---

================================================================================
CURRENT ISSUE (historical — resolved above)
================================================================================
SYMPTOM: In the HOLD/lift test the drones track perfectly at first, then one or
more "slide away" radially outward (rdrift grows), attitude rate commands rail at
+/-1.0, they lose altitude and crash. The load overshoots its target.

WHAT IS NOW PROVEN (via controlled experiments):
- The PLANNER/reference is correct: offline OCP converges, tension & FF match
  analytic values, drone ref positions are geometrically consistent.
- The TRACKER is the failure: given a perfect, constant, feedback-free reference
  to simply HOLD, it still goes unstable and rails the attitude loop.
- Instability tracks |aC| (the applied cable feedforward): with a_cable OFF the
  hold is far more stable; the moment the modelled cable force reaches full
  magnitude (~3 m/s^2) the tracker destabilises — EVEN when the cable is really
  loaded. Hypothesis: soft cable => real tension varies with position, but we
  feed a CONSTANT open-loop force across the horizon -> feedback mismatch the
  short-horizon RTI can't hold.
- Latest run: even cable-BLIND + gated thrust FF + gentle lift, one drone (d0)
  still slides out as the loaded thrust FF engages. So the loaded tilt/throttle
  FF itself (not only a_cable) is part of it. Asymmetry (d0 fails first) is
  unexplained.

================================================================================
EVERYTHING TRIED (chronological, most recent last)
================================================================================
- Added attitude+thrust feedforward from planner (was missing -> tilt & fall).  [helped]
- Added a_cable term to the tracker MODEL (not just cost).                       [see issues]
- Tautness gate on cable LENGTH to avoid feeding tension while slack.            [wrong metric]
- Coupled online OCP: more SQP iters + recovery/reseed on failed solve.          [still diverged]
- Replaced positive-feedback lift target with a time-based ramp.                 [fixed 1 bug]
- Two-phase creep takeoff (rise, anchor xy to spawn) for the slack world.        [partial]
- Switched to three_soft_paper.sdf (elevated/taut) to isolate steady hover.      [good test bed]
- FOUND: planner geometry hard-coded cable_len=0.6 vs world 1.0 -> every ref off.[major fix]
  Made geometry ROS params; added start_taut (skip creep) + diag_equilibrium.py.
- FOUND: a ZOMBIE planner from an old launch was co-publishing and corrupting
  every run. Added preflight_quad_load.sh + publisher-count check.               [major fix]
- Replaced coupled planner with OPEN-LOOP 'kinematic' generator.                 [reference now perfect]
- Gated lift ramp on TAKEOFF (/fleet/step>0) to stop pre-arm runaway.            [fixed a bug]
- Added exy/rdrift to diag (was z-only) -> could finally SEE the radial slide.
- Controlled A/B: attitude_ff OFF (no help) vs cable_ff_scale=0 (big help)
  -> localised the destabiliser to the a_cable model term.
- Gated a_cable on the LOAD being airborne (not cable length).                   [holds until load loads up]
- Gated the WHOLE thrust FF (unloaded level -> loaded tilted) by the same gate.  [still slides on load-up]

STILL OPEN: even with a provably-blind model and load-gated FF, the loaded thrust
FF + real soft-cable coupling destabilises at least one drone as the payload's
weight transfers onto the cables.

================================================================================
LIMITATIONS — WHY THIS DOES NOT (YET) OPERATE LIKE THE TU DELFT PAPER
================================================================================
1. LOW-LEVEL CONTROLLER: MPC @ 50 Hz vs the paper's INDI (inner rate loop 300 Hz,
   outer 100 Hz). INDI reacts to a MEASURED external force every inner tick; our
   MPC re-plans a horizon at 50 Hz. Bandwidth is likely the real limiter for
   rejecting the fast cable disturbance.
2. CABLE FORCE SOURCE: we feed an OPEN-LOOP MODELLED cable force (planner t*s/m).
   The paper feeds a MEASURED reactive f_ext from the IMU. Part 2 shows the model
   is ~2x too high in transient. The measured path is built but UNSTABLE when fed
   back (see Part 2 blocker) — the reactive inner loop is not yet working.
3. CABLE MODEL: our STABLE config uses RIGID massless links (matches the paper's
   modelling assumption and removes the soft-cable position-dependent spring that
   broke the tracker). Real cables are flexible with mass/drag/stretch — a genuine
   sim-to-real gap in BOTH directions (soft world was unstable; rigid world is an
   idealisation).
4. STATE ESTIMATION: we use Gazebo GROUND-TRUTH mocap for load pose AND cable
   directions. The paper runs an EKF fusing mocap + IMU to estimate load state and
   each cable's direction/rate. We have NO estimator — cable directions are read
   from perfect sim geometry.
5. PLANNER: 'kinematic' open-loop analytic lift (latch taut config, ramp up). The
   paper solves a full cooperative trajectory-optimisation online. Ours only does
   hover/vertical-lift; it CANNOT do the paper's agile maneuvers (figure-8, throw,
   pose changes). The 'coupled' online OCP we wrote diverges and is abandoned.
6. START CONDITION: our load starts on the GROUND (takeoff platforms, load-transfer
   transient). The paper's flight tests start from an already-airborne hover, so
   they never fight the ground-release impulse.
7. MISSING PHYSICS: no aerodynamic drag on drones/cables/payload, no cable mass,
   no wind/disturbance, no motor/battery sag. thrust_ratio=38 is an identified
   scalar gain, not a physical thrust curve.

================================================================================
REAL-WORLD TESTING — WHAT TO CHANGE (cable length, payload mass, etc.)
================================================================================
GOLDEN RULE: every physical quantity lives in BOTH a sim place and a
controller/param place. In the real world there is no SDF — you keep ONLY the
controller/param values, and they MUST equal the measured physical hardware.

CABLE LENGTH:
- Sim: generate_rigid_world.py --cable-len (or edit the .sdf tether geometry) AND
  attach geometry (--attach-radius, --attach-z, --elev).
- Controller: planner param cable_len (launch arg cable_len:=...), attach_radius,
  attach_z. THESE are what matter for real hardware — set them to the measured
  physical cable length and the payload attachment ring radius/height.
- Keep sim and param in sync or every reference is geometrically wrong (this bug
  cost us many runs: world 1.0 m vs planner 0.6 m).

PAYLOAD MASS (and inertia):
- Sim: payload <mass> and <inertia> in the world SDF.
- Controller: the load mass/inertia in controller_load_mpc/load_cable_dynamics.py
  (m_load, load inertia). Set to the measured payload. The per-cable hover tension
  scales as m_load*g/(n*sin(elev)); check with diag_equilibrium.py after changing.

DRONE MASS + THRUST:
- thrust_ratio (est_params[0]=38): the throttle->acceleration gain. For a REAL
  drone, IDENTIFY it: hover at steady throttle thr_h, then thrust_ratio = g/thr_h
  (our sim: 9.81/0.258 ~= 38). Wrong value => steady altitude bias (we saw ez~-0.3
  when the applied thrust didn't match the model).
- Per-drone mass m_i appears in the planner's cable_accel = t_i*s_i/m_i — set to
  the real drone mass (incl. battery + its share of the rig).

BETAFLIGHT RATE PARAMS:
- est_params = [mass?, drag, arm_len(0.12), rc_rate(70), super_rate(670), ...].
  The rate constants MUST match the FC's actual Rateprofile (Betaflight
  Configurator) or roll/pitch/yaw rate commands are mis-scaled. Re-identify per
  airframe.

IMU / MEASURED f_ext (Part 2):
- On real hardware the FC IMU already exists; publish its accelerometer as
  /drone_N/imu (sensor_msgs/Imu), body frame, gravity INCLUDED (reads ~[0,0,9.81]
  at rest, like Gazebo). Calibrate/remove bias. Keep the same EMA filter.
- measured_cable_accel() needs thrust_ratio and the applied throttle to be right,
  or the thrust it subtracts is wrong and f_ext is corrupted.

STATE / MoCap:
- Replace payload_mocap_emulator with the real MoCap feed publishing
  /drone_N/motion_capture_state and /payload/motion_capture_state.
- Real cable directions are NOT observed directly -> need the EKF (deferred) to
  estimate s_i, or a marker on each cable.

FLEET / TOPOLOGY:
- N_DRONES / DRONE_NAMES (launch), planner n, formation geometry, and the
  num_drones bridge arg must all agree.
- FREQUENCY_HZ must match across planner, tracker and fleet manager.

================================================================================
QUESTIONS FOR SUPERVISOR
================================================================================
- Is open-loop modelled cable force (from the planner) fundamentally the wrong
  approach for SOFT cables, given the paper uses MEASURED f_ext (IMU) + INDI?
  Should I bridge the drone IMUs and switch to a measured reactive force?
- Is 50 Hz MPC too slow vs the paper's >=100 Hz (INDI 300 Hz)? Is control-loop
  bandwidth the real limiter for rejecting the cable disturbance?
- For a thesis, is it acceptable to demonstrate the HIGH-LEVEL planner with a
  cable-blind / disturbance-rejecting tracker, and treat the cable-aware inner
  loop (INDI/f_ext) as future work? Or is the inner loop essential to the claim?
- Should the cables be modelled stiffer (more joint damping) or as rigid links
  to better match the paper's rigid-cable assumption, or is soft-cable realism
  a contribution?
- Is the load-transfer transient (payload lifting off the ground) worth solving,
  or should experiments start from an already-airborne/hovering load like the
  paper's flight tests?
================================================================================
